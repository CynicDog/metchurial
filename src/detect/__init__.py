# -*- coding: utf-8 -*-
"""Sensitive-column comparison detection: the tiered ANTLR parse driver,
finding extraction, and comment re-scanning. Known-name matching lives in
scan.py instead, alongside the orchestration that ties both detection
mechanisms together."""
