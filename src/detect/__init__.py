# -*- coding: utf-8 -*-
"""Rule 1 sensitive-value detection: the tiered ANTLR parse driver, HIT
extraction, and comment re-scanning. Rule 2's known-name matching lives in
scan.py instead, alongside the orchestration that ties both rules
together."""
